(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name app.components
     * @requires dependencies
     * @description
     *
     * The `components` description.
     *
     */
    angular
        .module('app.components', [
            'app.exhibition',
            'ui.bootstrap',
            'ngMaterial',
            'ngAnimate'
        ]);
})();
