(() => {
    'use strict';

    /**
     * @ngdoc module
     * @name layout
     * @requires dependencies
     * @description
     *
     * The `layout` description.
     *
     */
    angular
        .module('layout', [
            
        ]);
})();
